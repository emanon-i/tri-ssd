---
description: L1 要件から L2 システム構成（docs/l2_foundation/foundation.md、技術スタック・アーキテクチャ・テスト戦略）を対話的に生成する。
when_to_use: 技術選定・アーキテクチャ設計・foundation を作りたいとき。gen-l1（L1 要件）の後、gen-l3（フェーズ）の前に使う。
argument-hint: なし
allowed-tools: Read, Write, Edit, Glob, Grep, WebSearch
---

# L2 生成コマンド

<tri_ssd_context>
Tri-SSD（Tri-Layer Slice Spec Driven）はAI/LLMコードエージェントを前提とした仕様駆動開発。

レイヤー構造:
- L0: アイディア・ラフメモ（docs/l0_ideas/）- 任意
- L1: 要件（docs/l1_requirements/vision.md）
- L2: システム構成（docs/l2_foundation/foundation.md）
- L3: フェーズ（docs/l3_phases/PH-xxx.md）- 機能+受け入れ条件

ID形式: PREFIX-nnnn（REQ, PH, F）
</tri_ssd_context>

## 概要

L1（要件）から L2 システム構成（foundation.md）を生成する。
技術選定はユーザーとの対話を通じて行う。

## 生成時の原則

<avoid_over_engineering>
- 直接要求されたか、明確に必要な技術選定のみ行う
- シンプルな技術スタックを優先する（複雑な構成は避ける）
- 要件の規模に見合った技術を選定する（個人プロジェクトにエンタープライズ構成は不要）
- 「将来必要になるかも」で技術を追加しない
</avoid_over_engineering>

## 引数

- 引数なし（L1から対話的に技術選定を行う）

### 使用例

```
/gen-l2
```

## 前提処理

1. `docs/l1_requirements/vision.md` を読み込み、要件を把握する
2. `docs/l2_foundation/foundation.md` が存在するか確認（再生成モード判定）

---

## 出力フォーマット（必須）

### 必須構造

```markdown
# [プロダクト名] システム構成

## 1. 技術スタック
### 1.1 フロントエンド
### 1.2 バックエンド
### 1.3 データベース
### 1.4 インフラ
（各カテゴリで選定理由を記載）

## 2. アーキテクチャ
### 2.1 設計方針
- 採用パターン: [例: Screaming Architecture + DI]
- 選定理由: [プロジェクト規模・要件に基づく説明]
- ファイル粒度: [例: ドメイン単位でまとめる。インターフェースは外部依存の差し替えが必要な箇所のみ]
### 2.2 コンポーネント構成図（ASCII図）
### 2.3 ディレクトリ構成

## 3. 非機能要求（省略可）
- 性能: ...
- セキュリティ: ...
- 可用性: ...

## 4. 用語集（省略可）
| 用語 | 定義 |
|------|------|

## 5. テスト・検証戦略
### 5.1 テストフレームワーク
| 種別 | ツール | コマンド |
|------|--------|---------|
| ユニットテスト | [例: jest] | [例: npm test] |
| Lint | [例: eslint] | [例: npm run lint] |
| E2E | [例: Playwright] | [例: npx playwright test] |

### 5.2 実装後の自己検証手段
| 検証対象 | コマンド/手順 | 期待結果 |
|---------|-------------|---------|
| ユニットテスト | [例: npm test] | 全テストパス |
| Lint/型チェック | [例: npm run lint] | エラー0件 |
| ビルド | [例: npm run build] | 正常終了 |

### 5.3 検証で使えるツール（省略可）
- [例: curl, httpie, Playwright, Cypress 等]

### 5.4 カバレッジ基準（省略可）
- [例: 行カバレッジ80%以上]
```

**必須**: セクション 1, 2, 5
**省略可能**: セクション 3, 4（該当なしなら省略可）

## 再生成モード（既存ファイルがある場合）

既存の `foundation.md` がある場合は**再生成モード**で動作する。

### 保持するもの（上書きしない）

| セクション | 理由 |
|-----------|------|
| 採用しなかった選択肢 | 過去の検討結果は資産 |
| ユーザーが追記したメモ | 手動編集を尊重 |

### 更新するもの

| セクション | 条件 |
|-----------|------|
| 技術スタック | L1変更または明示的な変更要求時 |
| アーキテクチャ | 技術スタック変更に伴う場合 |

### 再生成時の手順

1. 既存ファイルを読み込み、各セクションの内容を把握
2. L1との差分を分析
3. ユーザーに「何を更新するか」を確認
4. 保持セクションはそのまま、更新セクションのみ再生成

## 生成手順

### Step 1: L1分析

- 要件を把握
- 用語の抽出
- 非機能要件の抽出

### Step 2: 技術選定（対話型）

WebSearchで以下のカテゴリの候補を**最低3個**ずつ**並列で**検索:
- フレームワーク（フロント/バック）
- データベース
- 認証方式（必要な場合）
- インフラ/デプロイ先

<thinking_process>
技術選定の判断時は、以下の形式で思考を整理してから結論を出す：

1. **要件の確認**: L1で求められている規模・性能・セキュリティレベル
2. **候補の評価**: 各候補の長所・短所をL1要件に照らして評価
3. **チーム適合性**: ユーザーの技術スタック経験を考慮
4. **判断**: 最もシンプルかつ要件を満たす選択肢を推奨
</thinking_process>

ユーザーに選択肢を提示し、対話を通じて選択してもらう。

### Step 3: アーキテクチャ設計

#### アーキテクチャ3原則（規模を問わず意識する）

| 原則 | 目的 | 具体例 |
|------|------|--------|
| 依存性の逆転 | テスト容易性・外部依存の差し替え | DB/APIへの依存をインターフェース経由にする |
| 境界の明確化 | 並列開発・影響範囲の限定 | 機能/レイヤーごとにディレクトリを分離 |
| インターフェース安定性 | 要件変更時の影響最小化 | 内部実装の変更が外部に漏れない設計 |

#### 推奨アーキテクチャ

**Screaming Architecture + DI** をデフォルトとする:
- フォルダはドメイン単位（orders/, users/）で構成する
- 外部依存（DB, API等）はDIで注入し、テスト時に差し替え可能にする
- shared/ は横断的関心事（認証、ログ、DB接続）のみ。2ドメインで共有する程度なら各ドメインに複製する方が安全

※ 大規模（複数チーム）の場合: 各ドメインフォルダ内に ports/adapters を追加検討

L1の要件規模を踏まえ、ユーザーと相談してパターンを調整する。

#### 定義する内容

- 設計方針（採用パターンと選定理由）
- コンポーネント構成図（ASCII図）
- ディレクトリ構成（プロジェクト構造）

#### 過剰分割の防止

<avoid_over_fragmentation>
**ファイル分割 — やらないこと**:
- 実装が1つしかないインターフェースを作らない（テスト差し替えが必要な外部依存のみ例外）
- 全操作を UseCase/Service クラスに通さない（単純CRUDはリポジトリ直接呼び出しで良い）
- レイヤーごとに DTO + マッパーを作らない（変換が不要ならドメインモデルを直接渡す）
- 10-20行程度のファイルを量産しない（1ファイルに複数の関連する型/関数をまとめて良い）

**関数化 — やらないこと**:
- 1-2箇所からしか呼ばれず外部公開もしない処理を関数に切り出さない（インラインで書く）
- 関数化の前に、その関数自体が肥大化していないか確認する（300行超の関数は設計を見直す）

**関数化して良い場合**:
- 3箇所以上から呼ばれる、または外部に公開する
- 現時点で1-2箇所でも、仕様上明確に今後呼び出し箇所が増えるとわかっている

**判断基準**:
- 新機能追加時のファイル数が機能の複雑度に比例しているか
- 単純なCRUD機能で5ファイル以上必要なら過剰分割の兆候
</avoid_over_fragmentation>

#### 設計後の確認観点

以下を満たすか確認し、満たさない場合は設計を調整する:
- 調査と実装を並列に進められる境界があるか
- 要件が変わったとき、影響が局所化されるか
- 各コンポーネントを単独でテストできるか
- 単純な機能追加で不必要に多くのファイルが必要にならないか

### Step 4: 非機能要求整理（必要な場合のみ）

L1の非機能要求を具体化:
- 性能（レスポンス時間等）
- セキュリティ（認証・認可方式）
- 可用性（SLA等）

### Step 5: テスト・検証戦略

選定した技術スタックに対応するテスト・検証ツールを整理:
- 技術スタックから自明なテストフレームワークを記載（例: React → jest/vitest）
- L1で性能要件やAPI仕様がある場合、検証ツールも記載（例: curl, k6）
- **L3の受け入れ条件でエージェントが自律検証するための基盤**となる
- **実装後の自己検証手段**を必ず定義する（コマンド + 期待結果のペア）

## 出力ファイル

| ファイル |
|---------|
| `docs/l2_foundation/foundation.md` |

## 完了後の案内

- 生成ファイルのパスを報告
- **`/gen-l3`** でL3フェーズを生成することを案内

---

## エラーケース

| ケース | 対応 |
|--------|------|
| L1が存在しない | エラー: 「L1が見つかりません。`/gen-l1` を先に実行してください」 |
| L1が作成途中 | 警告: 「L1が作成途中の可能性があります。続行しますか？」 |
